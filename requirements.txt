# 运行时依赖（Python 3.10-3.11）
#
# LangChain 这一组锁到本地实测通过的版本：只写范围时，pip 会在
# langchain / langsmith / openai 之间反复回退试探版本，线上构建实测
# 从 3 分钟拖到 10 分钟以上（魔搭创空间构建日志 385 秒仍卡在同一步）。
langchain==1.3.18
langchain-core==1.6.4
langchain-openai==1.6.3
langchain-chroma==1.1.0
langsmith==0.11.2
openai==3.6.0
chromadb==1.5.9

# gradio 保留范围：魔搭创空间基础镜像自带 5.12.0，本地/Docker 装 5.50.0，
# 两种版本本项目都兼容，锁死反而会强制镜像升级、拖慢构建。
gradio>=5.0,<6.0
ddgs>=9.0
pyyaml>=6.0

# 测试
pytest>=8.0
