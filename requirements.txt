# 运行时依赖（Python 3.10-3.11）
#
# 这一组必须锁死，且要按「魔搭创空间的阿里云 PyPI 镜像里有的版本」来锁：
#   1. 不锁时 pip 会在 langchain / langchain-core / langsmith 之间反复回退试探，
#      实测构建卡在 pip 步骤 385 秒以上并最终失败；
#   2. 镜像比 PyPI 滞后几天，langchain 1.4.x 要求 langchain-core==1.6.4，
#      而镜像上最新只到 1.6.3，所以这里锁定 1.3.18 + 1.6.3 这组可解析组合。
# 版本组合已用 `pip install --dry-run --index-url mirrors.aliyun.com/pypi/simple` 验证。
# 注意：改动本文件会触发创空间重新构建镜像（约 3-7 分钟）。
langchain==1.3.18
langchain-core==1.6.3
langchain-openai==1.6.2
langchain-chroma==1.1.0
chromadb==1.5.9

# gradio 必须锁 5.50.0：魔搭创空间基础镜像自带的 5.12.0 在生成 Blocks 应用的
# /api_info 时会抛 `TypeError: argument of type 'bool' is not iterable`
# （gradio_client 的 JSON Schema 转换 bug），表现为「Use via API」和程序化调用直接 500。
# 5.50.0 已在本地与线上实测通过。
gradio==5.50.0
ddgs>=9.0
pyyaml>=6.0

# 测试
pytest>=8.0
